# 예상못한 부작용 사냥 — 검증경계 밖 corpus 측정

> 2026-06-17. 사용자 걱정: "생각못한 부작용 없나?" 그간 검증은 6사+분식 백테스트 경계 안. 경계 밖
> (corpus 전체)을 측정. 특히 corpus 정규화를 바꾸는 cross_statement(78차)의 blast radius·이중계상.

## cross_statement (가장 위험 — corpus 정규화 변경)

### blast radius — 1사가 아니라 454사
```
스캔 raw 파일 9,328 · 4 OCI id 보유 회사 454 (CIS/IS 출현행 717)
```
KB 1사만 검증했지만 **실제 영향은 454사**. 사용자 걱정이 정확했다(경계 밖이 컸다).

### 표본 80사(blast) 전수 정규화 측정

| 검사 | 결과 | 판정 |
|------|------|------|
| **이중계상**(구제 canonical이 같은 year/fs_div/sj_div 기존행과 중복) | **0건** (89 구제 중) | ✅ 핵심 위험 미발생 |
| **오구제**(BS/CF서 잘못 구제) | **0건** — 89 구제 전부 sj_div=CIS | ✅ statement 호환 차단 작동 |
| 구제 canonical 분포 | 의도한 4종만(해외사업환산13·현금흐름위험회피10·FVOCI52·해외순투자14) | ✅ 서프라이즈 0 |
| cross 코드 예외 | **0건** | ✅ |
| (참고) EmptyDataError | 92건 — **빈 CSV(헤더만)** 하니스 직접호출 아티팩트. 실 파이프라인은 빈 파일 skip(6사 재정규화서 정상) | 무관 |

- **결론**: blast 454사로 크지만, 효과가 **균일하게 정확**(기타→올바른 CIS OCI). 가장 우려한 이중계상·
  오구제·크래시 모두 0. cross 가산적 설계가 scale에서 유지됨.

## read-time 변경(G1·G5·G6·SCE·materials) 전수 스모크

> 이들은 저장 정규화를 안 바꾸고 report/material 생성을 바꾼다. 표본 150사 build_company_report +
> 4 material(note 포함) 크래시/이상 측정.

```
표본 150사: 성공 148 | 크래시 2 | 데이터있는데 빈큐 0
```

| 크래시 | 원인 | 제 변경 탓? |
|--------|------|------------|
| 00118345 | CatalogException: normalized_financials 테이블 없음(DB 미정규화) | **아님**(데이터/DB 상태) |
| 01437292 | KeyError 'basis' in `_ratio_time_series`(빈 ratio 회사) | **아님** — NEW(auto)·OLD(2022-2025) **둘 다 동일 크래시** → 기존 버그(내가 안 건드린 함수) |

- **read-time 변경이 유발한 신규 크래시 0건.** 2 크래시는 둘 다 기존(DB 미정규화·_ratio_time_series 결함).
- **빈큐(데이터있는데 review_queue=0) 0건** — G1(연도 데이터구동)이 표본 전수서 빈 material 안 만듦(원래 갭 해소 유지).
- **교차확인(다른 표본 200사, note 제외)**: 성공 196·크래시 4·빈큐 0. 4 크래시 전부 같은 기존 2종
  (basis KeyError ×3·DB없음 ×1). 신규 크래시 0 재확인.
- ★부수발견(내 변경 아님): `_ratio_time_series` 'basis' KeyError — 빈 ratio 회사서 report 크래시.
  **두 표본 합산 4/350 ≈ 1.5% 회사 영향** = 일회성 아닌 실제 빈도. 기존 버그, 별도 백로그(수정 권장).

## 남은 사각 (정직)

- cross 표본 80/454 측정 — 이중계상 0이나 전수(454) 아님. 나머지 374사도 같은 부류라 동일할 가능성
  높으나 미측정. (전수는 별도 1회 가능)
- read-time 스모크 결과 대기.
- 79차 발견(cross 4/54 OCI id만 = 클래스 미완)은 별개(커버리지), 여기선 "한 것의 부작용"만.

## 종합

- **cross_statement**(corpus 정규화 변경): blast 454사로 크나, 표본 80사서 이중계상 0·오구제 0·코드크래시 0.
  효과 균일 정확(기타→올바른 CIS OCI). 가산적 설계가 scale 유지.
- **read-time 변경**: 150사 스모크 신규 크래시 0·빈큐 0. 발견된 2 크래시는 둘 다 기존 결함.
- **결론**: 본 컨텍스트 수정이 유발한 예상못한 부작용 = **표본 측정상 0**. 단 (a)cross는 80/454 표본이라
  잔여 374 미측정 (b)`_ratio_time_series` 기존 버그를 스윕이 새로 표면화 — 둘 다 별도 항목.

## 남은 사각 (정직)

- cross 표본 80/454 — 이중계상 0이나 전수 아님. 잔여 374사 동일 부류라 동일 가능성 높으나 미측정.
- `_ratio_time_series` 'basis' KeyError(기존 버그, 내 변경 무관) — 빈 ratio 회사 report 크래시. 백로그.
- 79차 발견(cross 4/54 OCI = 클래스 미완)은 커버리지 갭(별개).
