# P1 라운드2 수정 프롬프트 — 핸드오프 (층화 표본 5사 감사 발견분)

> 라운드2(층화 5사·16 회사연도, seed=2, `_round_targets_round2.json`) 감사 발견 수정 지시서.
> 증거 전부 duckdb·raw 재현 검증(매트릭스 `_VERDICT_MATRIX_round2.md`, 게이트 PASS).
> 선행: [_P1_ROUND1_FIX_PROMPT.md](_P1_ROUND1_FIX_PROMPT.md)(N1~N5 완료). 형식·제약 동일.

## 표본

```
00688996 금융형(715.7조) 4년    01675421 별도전용 4년    00121686 blank고비중·금융 1년
00117577 다년대형(6.2조) 6년    00169215 단년소형 1년
```

라운드1 수정 효과 확인: 라운드2에서 소실 0·병합 0·N1/N2/N3/D5 재발 0. 이번 발견은 전부
SCE role/차감의 **라벨·canonical 변형**(데이터 등록 누락)과 **규칙 사각** 갈래다.

## 우선순위: R1(6개년 체계적) → R2(1.14조) → R4 → R3 → R5(하니스)

---

## R1. SCE 소계 라벨 변형이 role 분류 밖 → leaf로 이중계상

### 증거 (00117577 6개년 전부 — 검증 완료)
- `소유주와의 거래 합계`: canonical까지 exact 매핑됐는데 `sce_change_roles.subtotal` 목록에
  없어 **change_role=leaf** → 2024 차이 -80,708 = 정확히 이 bare 행 값(-80,707.8 재현).
- `총포괄이익`(총포괄손익의 라벨 변형)·`자본에 직접 반영된 소유주와의 거래 등`: unmapped
  leaf → 2020~2023 차이(157,084/376,137/254,107/194,859) 전부 이 두 행 합으로 분해.
- 같은 회사 2023의 `기타포괄손익`은 subtotal 정상 분류 — '이익/손익' 한 글자 차이가 명암.

### 수정 방향 + 수용 기준
- subtotal role 판정을 canonical 정확 일치 + **라벨 변형 흡수**로: `총포괄이익`을 총포괄손익
  alias 등록, `소유주와의 거래` 계열(…합계/…등)을 소계로 일반 등록. 키워드 마커 규칙
  ('소유주와의 거래'+'합계|등')도 허용 — 단 config 데이터로(코드 하드코딩 금지).
- 수용: 00117577 6개년 검산 전부 OK.

## R2. 통합 canonical `자기주식변동`이 차감 목록 밖 + 무조건 -abs 불가

### 증거 (00688996/2023 — 검증 완료)
- raw `ifrs-full_PurchaseOfTreasuryShares` '자기주식 취득' **+571,745**(양수) → canonical
  `자기주식변동`(통합) 매핑. `sce_deduction_changes`엔 `자기주식취득`만 있고 `자기주식변동`
  없음 → -abs 미적용, 자본 증가로 합산 → 차이 1,143,490(부호 오류라 오차 2배).
- 주의: `자기주식변동` alias는 취득·처분 **양방향** — canonical 통째 -abs 등록은 처분(+)을
  뒤집는 역버그. CFS/OFS 각 1행은 정상(중복 아님 — 검증됨).

### 수정 방향 + 수용 기준
- 차감 판정을 canonical 단일 키에서 **canonical+label 2단**으로: 통합 canonical은 label 의미
  (취득·소각·감자 → -abs / 처분·발행·재발행 → +abs)로 분기. 분기 사전은 config 데이터.
- 수용: 00688996/2023 검산 OK + 자기주식 **처분** 사례(기존 19건·라운드1·2에서 grep로 찾아
  최소 1건)가 +로 보존됨을 함께 확인(역버그 방지, ripple-search).

## R4. total 라벨 변형 `기말자본` 미등록 → 검산 불가

### 증거 (00121686/2025)
- blank-id '기말자본'(bare 1,001,507)이 `자본총계`로 매핑 안 돼 '기타 중요 계정' leaf 잔류
  → total 식별 실패로 검산 불가. 등록 없이 두면 향후 total이 잡히는 회사에서 leaf 이중계상
  위험도 있는 행.

### 수정 방향 + 수용 기준
- `기말자본`(및 `기초자본` 대칭 변형)을 SCE total/opening alias로 config 등록 + role 매핑.
- 수용: 00121686/2025 검산이 "불가"에서 OK 또는 정직 FAIL(수치 노출)로 전환.

## R3. leaf 부재 소계의 변동 누락 (D5의 역방향)

### 증거 (00169215/2025)
- 회사가 OCI를 `기타포괄손익` 집계 1행(-296, subtotal 정상 분류)으로만 공시(구성 leaf는
  CIS에만). 검산이 subtotal 일괄 제외 → -296 누락, FAIL(296).

### 수정 방향 + 수용 기준
- 검산 계층 보정 규칙: **subtotal의 bare leaf 자식이 0개면 그 subtotal을 leaf로 채택**
  (D5 이중계상 방지와 양립하는 일반 규칙 — 자식이 있으면 제외, 없으면 채택).
- 수용: 00169215/2025 검산 OK + D5 사례(00120526 중첩 소계 등) 무회귀.

## R5. 하니스 사각 — §I 병합 가시화가 dedup 후 집계 (하니스 수정)

### 증거 (01675421/2023)
- OFS BS 24→23: `보통주자본금`(dart_IssuedCapitalOfCommonStock, 431백만)이 동액 `자본금`과
  병합 collapse — 값 왜곡 0이지만 §I는 "다중 label 없음"(dedup 후 DB만 보기 때문).

### 수정 방향 + 수용 기준
- `_p1_company_review.py` §I를 dedup 이전 단계 기준으로 보강: raw를 매퍼로 매핑해
  canonical별 distinct raw label(≥2)을 세고, DB 생존 label과 대조해 "병합으로 사라진 label"
  을 표시. (§D의 매퍼-미경유 원칙은 '값 대조'에 한정 — 병합 '가시화'는 매퍼 사용이 목적
  그 자체이므로 정당. 주석으로 구분 명시.)
- 수용: 01675421/2023 §I에 보통주자본금 collapse 노출 + [기계요약] 병합 카운트 반영.

---

## 부수 기록 (수정 대상 아님 — 별도 추적)

- **수집 갭 반복(A6)**: 00688996·01675421 2020~2022 raw 헤더만(라운드1 00117267과 동일,
  금융지주/신규상장 계열 3사째). 00121686/2025는 notes_xbrl 디렉터리 자체 부재(주석만 갭).
  → 수집 레이어 점검 과제로 누적(라운드 종료 후 일괄).
- 부호반전 7건 전부 차감 -abs 의도 내(거짓 경보 확인). 00117577 매각예정자산 별도표시·
  주석 적재율 연도 편차(17~20% vs 72~73%)는 원천 특성/축 부족 — 관찰.
- Phase2 관찰 후보(분석 단서, 수정 아님): 00121686 흑자전환+재고급증, 00169215 적자전환+CB,
  00117577/2024 파생평가익 152,800.

## 검증 프로토콜 (순서 고정)

1. TDD: R1(소계 변형 role)·R2(label 분기 차감)·R3(계층 보정)·R4(alias) 실패 테스트 먼저.
2. 수정 → **라운드2 5사 + 라운드1 5사 + known 19** 재정규화(--force).
3. 배치 3개 전수: known·round1(`_round_targets.json`)·round2(`_round_targets_round2.json`) —
   라운드2 검산 FAIL 8건+불가 1건 해소, round1·known 무회귀(특히 D5·N1~N3 재발 0).
4. 백테스트 recall 5/6 유지. pytest 통과. ruff·인코딩 깨짐 0.
5. round2 매트릭스 삭제 후 재생성→통독→게이트 PASS.
6. 문서: PROTOCOL(B3·D5·D6 상태, 발견 이력 — 라운드2 항목은 이미 있음, 수정 결과만 추가),
   STATE 갱신.

## 제약 (글로벌 룰)

- 라벨·canonical은 **config 데이터 등록**(코드 하드코딩 금지). 한글 파일 최소 diff.
- 커밋은 사용자 명시 요청 시만. §9 수치 직접 재현 후 완료 선언.
- 검증 2+ 케이스: R2는 취득(00688996)과 처분 사례 양방향 필수.
