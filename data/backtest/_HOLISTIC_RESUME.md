# 재개 지침 — 전수 홀리스틱 재검 공장 (컨텍스트 컴팩트 후 이것부터 읽고 실행)

> 2026-06-14 셋업. 사용자가 컴팩트 후 이어감. 이 파일대로 즉시 실행하면 됨(재설계 불필요).

## 무엇을 하는 중인가
라운드1~12 동안 기계검사(SCE 검산·소실 등 식)가 띄운 숫자 FAIL만 역추적하고 **감사관 통독을
사실상 안 한** 사각을 메운다. 감사한 101개사/383 회사연도를 **LLM이 감사관처럼 전수 통독**해
식이 못 본 것(의미적 오매핑·급변 비정합·주석 분식신호)을 잡는다.

## 셋업 완료물 (이미 디스크에 있음)
- 분할: `data/backtest/_holistic_chunks.json` — **34묶음**, 회사단위 무겹침(합집합 383·중복 0).
- 공장 프롬프트: `data/backtest/_HOLISTIC_AUDIT_PROMPT.md` — 6렌즈·flag 추적 금지·발견을
  [P1결함]/[원공시]/[P2후보]로 분류·산출은 `_holistic_findings/chunk_<ID>.md`.
- dump 현행화 배치: **✅ 완료(2026-06-14, exit 0)** — 383 dump 전수 현행화됨(R12 반영).
  바로 wave 발사 가능.

## 재개 절차 (순서대로)
1. **dump 완료 확인됨** — 재확인 불요. 곧장 2번으로.
2. **wave 1 발사 (묶음 1~8).** Agent 툴 8개를 **한 메시지에 동시** 호출(병렬). 각 호출:
   - `subagent_type`: `general-purpose` (p1-auditor는 flag 편향 시스템프롬프트라 피한다 — 홀리스틱엔 중립 에이전트)
   - `prompt`: "`data/backtest/_HOLISTIC_AUDIT_PROMPT.md`를 읽고 그대로 수행. **CHUNK_ID = N**.
     작업 디렉터리 C:\Users\ghdtj\workspace\portfolio\fs-multi-analyzer." (N=1..8)
3. **깊이 검수.** wave 1 결과(`_holistic_findings/chunk_1~8.md`) 중 1~2개를 직접 열어 6렌즈가
   실제 적용됐는지(도장 아닌 근거 인용) 확인. 얕으면 프롬프트 보강 후 재발사.
4. **나머지 발사 (묶음 9~34).** 웨이브당 ~8개씩(동시성 한계), 3~4 웨이브.
5. **종합.** 전 findings를 모아 [P1결함]을 추려 수정 프롬프트 후보로, [P2후보]는 Phase2 인계
   목록으로 정리. 식이 놓친 신규 패턴이 있으면 그게 이 작업의 핵심 산출.

## 주의 (사용자 지정)
- 회사/연도 안 겹치게 — 분할이 이미 보장(검증됨). 묶음 id로만 호출하면 자동.
- LLM이 깊게 판단하게 — 묶음당 ~10 회사연도(깊이우선), 중립 에이전트, flag 추적 금지 프롬프트.

## 동시 진행 중인 별개 트랙 (혼동 금지)
- `_P1_MEMBER_SIGN_FIX_PROMPT.md`(병렬화+member 부호) — 발행됨, 외부 실행 대기. 홀리스틱과 별개.
- 라운드13(20사 재검, 구조규칙 수렴 확인) — member 부호 수정 후. 홀리스틱과 별개.
- 수렴 사다리: 10사✅→20사(R11/R12에서 신규 검출, 미통과)→50→100.

## ★미답 글로벌 검사 트랙 (공장으로 못 함 — 별도 한 방 스크립트 필요, 잊지 말 것)
PROTOCOL 28항목 전수 대조(2026-06-14) 결과 per-chunk 홀리스틱으로 **못 잡는** 글로벌 구멍:
1. **F1 신호 dangling (★PROTOCOL 최우선, 한 번도 안 함)** — lump/rename로 canonical 이름이
   바뀌었는데 `src/signals/`가 옛 이름을 참조하면 분식 신호가 조용히 죽는다. 검사: 신호엔진이
   참조하는 canonical 문자열을 전부 추출 → 현 `config/canonical_accounts.yaml`에 존재하는지 대조.
   **가장 위험한 미답 사각.** 일회성 스크립트로 즉시 가능.
2. **IS·CIS·CF 산술검산 부재** — BS(항등식)·SCE(roll-forward)만 산술검사가 있고 손익·현금흐름은
   "숫자 존재(소실)"만 본다. 추가: IS 매출−원가−판관비−영업외=순이익 체인, CF 영업+투자+재무=
   현금순증감. 기계 floor 확장(하니스 §B에 IS/CF 검산 추가).
3. D6 회사간 비교가능성·F3 결정론 재현성(2회 정규화 동일) — 우선순위 낮음, 글로벌.
홀리스틱 wave와 **병행**해 F1(최우선)·IS/CF 검산부터 처리.
