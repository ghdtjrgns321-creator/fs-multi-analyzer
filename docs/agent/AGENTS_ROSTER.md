# 현재 LLM 에이전트 명단·기대 역할

> 작성 2026-06-30. 운영 경로에서 실제 LLM을 호출하는 에이전트 전수(measure 실측 10회 호출과 일치).
> 역할 출처: `config/playbooks/perspective_prompts.yaml`(관점·반박) + 각 모듈. 현상태는 대주산업 실측.

## A. 온보딩 단계 (3개) — 데이터 준비·게이트

| #   | 에이전트        | 코드                                 | 기대 역할                                                                                                       | 현상태(대주)    |
| --- | --------------- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------- | --------------- |
| 1   | **S7 청크선별** | `src/report/review_chunks.py:105`    | 사업보고서 **본문(비재무 서술)**에서 소송·특수관계·우발·약정 등 "검토 관심 공시 종류"를 선별해 note 관점에 넘김 | 작동(청크 선별) |
| 2   | **alias 제안**  | `src/report/alias_suggest.py:155`    | 미매핑 계정에 표준 계정 **별칭(alias)** 추천 → 매핑 보강                                                        | 작동(5건 제안)  |
| 3   | **G6 홀리스틱** | dashboard 온보딩(측정 하니스 인라인) | Phase1 감사 dump 전체를 읽고 **소실·이질병합·검산불일치·이상부호를 감사관처럼** 열린 눈으로 판단                | 작동            |

## B. Phase2 의심건 — 관점(perspective) 에이전트 6개

> 코드: `src/report/perspective_runner.py:68`(통합 에이전트 팩토리) + `perspective_prompts.yaml`의 관점별 focus.
> **6개가 병렬 실행**되어 각자 의심건(SuspicionItem)을 냄 → 근거검증(grounding) → 카드.

| #   | 관점               | 기대 역할                                                  | 받는 입력(핵심)                                            | 현상태(대주 실측)                                    |
| --- | ------------------ | ---------------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------- |
| 1   | **numeric**(정량)  | 계정 금액의 수준·전년대비 증가율·구성비 이상               | account_metrics_panel(전 계정 계산값)·비율·signal_snapshot | ✅ 충실 9건, 주력                                    |
| 2   | **note**(정성)     | 주석·본문의 우발부채·약정·소송·특수관계·담보·계속기업 서술 | note_sections·report_review_chunks(S7 산출)                | ✅ 충실 8건                                          |
| 3   | **flow**(관계)     | 계정 **간 관계 모순**(매출↔채권↔영업CF, 이익↔법인세)       | signal_snapshot(관계신호)+panel                            | △ 관계재료 받으나 panel 급변에 묻혀 numeric 67% 중복 |
| 4   | **change**(시계열) | 전기 대비 급변 + **소급재작성** 패턴                       | panel의 yoy·trend·z + restatement 신호                     | △ 재작성 0건이라 numeric 63% 중복                    |
| 5   | **external**(외부) | 외부 맥락(뉴스·공시 외) — **출처 URL 필수**                | 외부 검색쿼리 재료(실 근거 수집 파이프 미구현)             | ❌ 소스부재 0건                                      |
| 6   | **industry**(동종) | **peer 벤치마크** 대비 비정상(참고 신호)                   | peer baseline·비율                                         | ✅ 충실 4건                                          |

## C. Phase2 메타 — 반박 에이전트 1개

| #   | 에이전트           | 코드                        | 기대 역할                                                                                                                                                        | 현상태           |
| --- | ------------------ | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| 7   | **rebuttal**(반박) | `src/report/rebuttal.py:43` | 제기된 카드마다 **정상 설명 가능성·반대근거·확인질문·다음절차**를 채우고 verdict(normal_dominant/mixed/suspicion_dominant) 부여. 정상우세는 하단 강등(제거 아님) | 작동(15 entries) |

## 운영 에이전트 총계 = 10개
온보딩 3 + 관점 6 + 반박 1 = **10** (measure LLM 호출 10회와 정확히 일치).

## 참고 — 코드에 있으나 운영 미사용(레거시)
`card_pipeline`이 import하지 않음 → 현재 파이프라인에서 안 돎:
- `src/agents/note_analyst.py`·`src/agents/numeric_analyst.py` — 관점별 개별 에이전트(통합 `perspective_runner`로 대체됨)
- `src/report/perspectives.py` — 구 관점 스키마/경로
- `src/report/external_agentic.py` — external 쿼리생성/평가(현 external 재료는 통합 관점이 처리)

## 설계 방향 연계
`HANDOFF_ROOT_REDESIGN.md`: 내부 3관점(numeric·flow·change)이 같은 CFS 본문 패널을 봐 겹침 →
관점을 **데이터 차원**으로 분리해야(flow=별도/관계, change=신규발생/소급, external=외부 파이프)
6관점이 제값. 현재는 실질 독립 기여 = numeric·note·industry 3개.
