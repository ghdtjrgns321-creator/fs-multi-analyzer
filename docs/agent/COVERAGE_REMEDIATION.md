# DART 커버리지 전수 보완 — 단계별 수행 계획

> 근거: [../../data/backtest/COVERAGE_AUDIT.md](../../data/backtest/COVERAGE_AUDIT.md) ·
> [COVERAGE_AUDIT2.md](../../data/backtest/COVERAGE_AUDIT2.md) 전수 감사.
> 원칙: **가성비/우선순위로 일부만 메우지 않는다.** 감사로 드러난 전 사각을 단계로 분해해
> 처음부터 하나하나 전수로 메운다. 각 단계는 의존순(수집 L0 → 정규화 L1 → 신호 L2 → 관점 L4)이며,
> 완료 기준은 **전수 재현값**(16 분식사 + 표본, 또는 전 수집사)이다. 새 에이전트(관점)는 만들지
> 않는다(PLAN §3 원칙2) — 데이터를 기존 6관점에 흘려보낸다.

## 현재 baseline (감사 확정값, Codex 정량은 자체 재측정으로 정정)

```
API        4 / 24 메서드 사용        (report 28·event 36·regstate 6·document·정정 = 0)
보고서      11011(사업)만            (반기·분기 11012~14 = 0)
재무제표    BS·IS만 신호             (CIS·SCE 전면 미사용, CF는 사슬만)
컬럼        9 / 18                  (S1 이후 frmtrm·bfefrmtrm 보존, ord·currency·thstrm_add 버림)
계정        신호화 17%               (floor미달 43%, 이력부족 17%, 임계미달 15%, 신규 8%)
주석        구조화 8종 수집(XBR103)   (원문·무형자산·개발비·관계기업 = 0)
재작성      frmtrm 버림 → 미탐        (자체 재측정: 16사 중 12사 BS 재작성 실재)
```

검증 앵커(하니스 신뢰): 삼성 2025 = 61계정/floor통과34/미달27 = 사용자 독립측정 일치.
frmtrm 자체 재측정: 아스트 자산 −997억·셀트리온 무형자산 −1,085억·이렘 매입채무 −627억(BS, [member]제외, 1억+1%).

---

## 단계 목록 (S0 → S10, 의존순)

각 단계: **무엇 / 산출물 / 전수 완료기준 / 상태**.

### S0. 감사 baseline·계획 고정 ⬅ 현재
- 무엇: 전수 감사 결과를 단일 현황표로 박고 단계 계획 수립(본 문서).
- 산출물: 본 문서 + COVERAGE_AUDIT(이미 존재).
- 완료기준: baseline 표 + S1~S10 정의 + 각 단계 전수 검증기준 명시.
- 상태: [x] 진행 중(본 문서 작성).

### S1. 행 필드 전수 — 전기/전전기 보존 (L1)
- 무엇: `finstate_all`의 `frmtrm_amount`(전기)·`bfefrmtrm_amount`(전전기)를 정규화 출력에 보존.
  `thstrm`(당기)은 기존 `amount`로 유지한다.
- 산출물: 정규화 frame에 전기/전전기 컬럼; `OUTPUT_COLUMNS` 확장.
- 완료기준(전수): 16사 전수 정규화 후 전기 컬럼 존재율 100%, 아스트 재고 2019 전기란 419억 보존 재현.
- 상태: [x] 완료(2026-06-07). 16개 positive 회사 재정규화 후 `prior_amount` 비결측 16,999/19,101(89.00%), `prior2_amount` 비결측 16,676/19,101(87.30%). 아스트 2020 OFS 재고 `prior_amount` 41,854,541,192원과 2019 OFS 재고 `amount` 110,270,586,075원 재현. 기본 labels 백테스트 5/6·5/6·5/6, mega 15/16·14/16·13/16 유지.

### S2. 소급재작성 신호 (L2, S1 의존)
- 무엇: N년 보고서 `frmtrm`(전기) vs N−1년 보고서 `thstrm`(당기) 대조 → 재작성 신호.
  [member]·차원행 제외, 절대1억+상대1% 임계.
- 산출물: `src/signals`에 재작성 탐지 + red flag; 6관점(change) 입력 연결.
- 완료기준(전수): 16사 전수 재작성 = 12/16 재현(모델솔루션·본느·이트론·에스엘 0 포함),
  셀트리온 무형자산 −1,085억 신호화. **BS 외 IS/CF 재작성도 측정 범위에 포함**.
- 상태: [x] 완료(2026-06-07). `src/signals/restatement.py` 추가. 전 `sj_div` 기준으로는
  15/16사 394개 신호가 발생했다. 기존 감사 baseline과 같은 BS line-item 기준은 12/16이며
  모델솔루션·본느·이트론·에스엘 BS 신호 0을 재현한다. 전 `sj_div` 확장 시 본느·이트론·에스엘은
  CIS/CF/SCE의 재분류·부호표시 변화가 1억+1% 임계를 넘는다. 앵커: 셀트리온 CFS 무형자산
  −108,537,593,133원, 아스트 CFS 자산총계 −99,677,813,878원, 이렘 CFS 매입채무
  −62,726,226,951원. 정상 10사는 전 `sj_div` 4/10사 19개, BS 0개.
- 보완(2026-06-07): 소계 제외, 단위/이상치 가드, 자산 1% floor, 중복 dedup을
  `restatement.py`에만 적용했다. 억제 후 정상 10사는 전 `sj_div` 2/10사 2개, BS 0개.
  16개 positive는 전 `sj_div` 15/16사 157개, BS 9/16사 33개다. 셀트리온 무형자산과
  이렘 매입채무는 유지, 아스트는 자산총계 소계 신호 대신 재고자산 −80,511,480,581원이 유지된다.
- 마무리(2026-06-07): restatement는 정상 소급 사유가 많아 결정론 queue에서 제외했다.
  `latest_signal_snapshot["restatements"]`와 change material 단서로만 유지한다. 정상 10사
  company report review_queue restatement 0건, 기본 labels 백테스트 5/6 유지.

### S3. 재무제표 5종 전수 — CIS/SCE 정규화·신호 (L1+L2)
- 무엇: CIS(포괄손익)·SCE(자본변동) canonical 등록 + 신호 스캔 sj_div에 CIS·CF·SCE 포함.
  현재 `universal.py:36` BS·IS만.
- 산출물: `canonical_accounts.yaml` CIS/SCE 계정; 스캔 sj_div 확장; CF 0근처 노이즈는 floor·base 가드.
- 완료기준(전수): 삼성·16사에서 CIS(OCI)·SCE(자본거래)·CF 전 계정이 신호 깔때기에 진입(현재 0 → 측정).
- 상태: [x] (구현 df4e1c8 6/7 + 45차 측정 확인). 분식 16사 신호 깔때기 sj_div별 측정:
  BS 55·IS 17·**CIS 10·CF 50·SCE 9**·미상 0 — 옛 BS·IS 전용일 때 CIS/CF/SCE=0이던 것이 양수 진입
  (CF 6사·CIS 3사·SCE 4사에서 발생, hollow 아님). 결정론 큐 채점은 BS·IS만, CF/CIS/SCE는 LLM
  관점 material로 격하(S2 "발견은 결정론·판단은 LLM" 패턴). canonical CIS 10·SCE 7 등록 완료.

### S4. 미매핑 보강 — IFRS16·투자부동산·관계기업 (L1)
- 무엇: 감사가 실명한 미매핑 핵심계정(사용권자산·리스부채·투자부동산·관계기업투자 alias버그)을 canonical로.
- 산출물: `canonical_accounts.yaml` alias 보강.
- 완료기준(전수): 표본 BS 미매핑율 48% → 측정·하락; 관계기업투자 alias 매칭 16사 확인(이렘 포함).
- 상태: [x] **종료(2026-06-15)**. 의미오매핑 correctness(원장 높음 13중 11 FIXED·BS 미매핑율 48%→38.3%·
  label_priority 일반규칙+교차표 가드)에 더해 S4 literal·잔여를 47차에 마감:
  - ①IFRS16: 유동/비유동리스부채 alias 보강(단기·장기·_유동/_비유동·괄호 변주). mapper 직접 재현 8/8 매핑.
  - ②관계기업투자: alias 보강(공동기업·종속기업 어순/공백 변주). 300사 raw 스캔 매핑 282건 확인.
  - ③잔여: member-sign 트랙 닫힘(audit 334→0·non-audit 진짜결함 0·00141477=원공시모순). **01406618=
    원공시 XBRL 오태깅(id↔label 정반대 4행, 올바른 canonical 모호)** → quirk 미적용(추측=조작), 알려진 한계로 기록.
  - 매핑 안 하는 게 옳은 것(contra '감가상각누계액(사용권자산)'·자본항목 '부의관계기업자본변동'·모호 '토지사용권')은 제외 유지.

### S5. 절대 수준 이상 신호 (L2) — 재정의 후 점검 완료
- 무엇: "변화"만 보는 현 신호에 "수준 이상"(DIO·부채비율·이자보상배율 등) 보강.
- ★재정의(42차): 결정론에 절대 임계를 박는 원안은 **폐기**. 근거 ①§3 — 업종 무시 절대임계
  일괄적용은 버그(조선·건설은 DIO·부채비율이 원래 높음) ②아스트는 결정론 변화신호로 이미
  discovered(백테스트 5/6) — 수준신호 없이도 recall 달성 ③문서 자체가 "절대 수준 이상(DIO) →
  업종 맥락·동종 비교로 LLM이 판단"으로 LLM 영역에 배치. 즉 수준 판단은 Phase2 LLM의 일.
- 점검 결과(42차, 코드 추적): 수준 판단 경로가 **이미 구현·배선됨**.
  - 비율 계산: `financial_ratios.yaml` 15종(DIO·부채비율·이자보상·자산회전율·유동비율 등) — 빠짐 없음.
  - 배선: `company_report._ratio_time_series` → `materials.py material_board["ratio_time_series"]`
    → `perspectives.create_perspective_assessment` 입력.
  - 지시: `perspectives.py` 프롬프트에 "ratio time series 능동 검토" + "근거 있는 **수준 이상**·추세
    이상 제기" + industry 관점 피어 baseline(업종 맥락) 명시.
  - 온보딩 통독 LLM(`run_llm_holistic`)은 **정합성 전용**이라 수준 이상치 미포함이 정상(역할 분리).
- 완료기준: 결정론 수준신호 추가는 불필요. 수준 판단은 Phase2 perspectives가 수행(배선·지시 확인됨).
- 상태: [x] (재정의 후 점검 완료 — 추가 코드 없음. 효과 측정은 Phase2 LLM 평가 트랙에서)

### S6. 분기·반기 수집 (L0) — 의도적 비범위(연간 전용)
- 무엇: `reprt_code` 11012~14 수집(현재 11011만).
- ★결정(43차): 풀 통합 **폐기**, 연간 전용 유지. 근거 ①분식 15/16은 연간에 흔적(recall 5/6 무분기)
  ②분기 재무는 누계→순분기 차감 도출 오류위험 ③비감사 분기는 정합성검증(BS항등식·SCE·주석적재율)
  노이즈 ④기간 축 추가는 수집~UI 전 레이어 복잡화 — 포트폴리오 핵심은 분석 아키텍처지 커버리지 아님(YAGNI).
- 남는 한계: "분기에 먼저 드러나는 기중 조작(4분기 몰아넣기·기중 결산수정)"은 미포착 — user 문서에 명시.
- 산출물: 한계 문서화 `docs/user/DATA_SCOPE.md` §2 "분기·반기를 넣지 않은 이유"(43차 추가). 코드 변경 없음.
- 상태: [~] 의도적 비범위(연간 전용, 확장 여지는 회사별 격리 수집으로 열어둠)

### S7. 사업보고서 원문 전체 주석 (L0+L1.5)
- 무엇: `document(rcept_no)` 원문 수집 → 무형자산·개발비·관계기업·특수관계자·세그먼트·리스 주석 파싱.
- 산출물: 원문 수집기 + 주석 인덱서 확장.
- 완료기준(전수): 16사 무형자산/개발비 주석 추출률; 셀트리온·이트론·웨이브 개발비 주석 확보.
- 상태: [ ]

### S8. 감사보고서 KAM·감사의견 (L0)
- 무엇: 감사보고서 수집 → 감사의견·핵심감사사항(KAM)·감사인 변경. external/note 관점 연결.
- 완료기준(전수): 16사 KAM 추출; 감사의견 비적정/한정 플래그.
- 상태: [~] **비범위(실측 결정 2026-06-15)**. 별첨 감사보고서 한계효용을 50 회사연도로 측정
  (`_S8_KAM_COVERAGE.md`·`_S8_ATTACHMENT_PROBE.md`): KAM·감사의견·감사인변경 요약은 **S7 PART V가
  이미 확보**(2019년 이후 전부 본문에 KAM, 별첨에만 KAM=0). 별첨이 유일 출처인 건 FY2018 과도기
  대형 분식사 2건(두산·셀트리온, KAM 도입 첫해 본문 서식 미반영)뿐이고, 그 KAM(분식영역 지목)은
  probe로 이미 추출했다. 운영(2019+ 분석)엔 PART V로 충분 → 별첨 별도 수집은 제품 기능 불필요.
  분식-era 백테스트가 필요하면 `document_all`로 일회성 추출(추가 구현 0). 수집 경로는 확인됨.

### S9. 정정공시 이력 (L0)
- 무엇: `list` 정정 추적 → 재무제표 정정 재제출 = 데이터 출처(원본/정정본)·무엇이 바뀜.
- 완료기준: 정정 이력 수집 + 재작성연도 플래그 + 화면 정정본 배지.
- 상태: **[x]**(2026-06-15, contract 63·64차). 잣대=분식 아닌 **데이터 출처**. `src/collect/correction.py`
  (parse A·정정사유/전후 헤더 B·collect_corrections) → spike `include_corrections` → `restated_years`
  → onboarding 정정본 배지. 규모 ~6,600 재무정정(`_S9_SCALE_COST.md`). 실데이터: 셀트리온 재작성{2016~2020}.

### S10. report 28종·event 36종 (L0)
- 무엇: 주요사항보고서 event(시점+조건) + 정기보고서 주요정보 구조화 수집.
- 완료기준: event 36+report 28 전수수집 + 타입→관점 라우팅 + compact material 투입.
- 상태: **[x]**(2026-06-15, contract 65·66차). 잣대=정보 완전추출. `src/collect/events.py`(36+28 수집,
  별도 참조저장=오염차단) → `event_routing.yaml`(타입→관점) → materials compact 타임라인(토큰차단)
  → 관점 LLM. 분석 `_S10_ANALYSIS.md`. 실데이터: 바이오 영업정지→numeric·CB→flow / 두산 합병→change.

### S11. PHASE1 마무리 — 결정론 최종 커버리지 검사 + Stage2(LLM) 인계 경계 ★종료 게이트
- 무엇: S1~S10 누적 적용 후, 결정론(L0~L2)이 **최종적으로 무엇을 잡고 무엇을 LLM에 넘기는지**를
  전수로 검사·고정한다. PHASE1(결정론) 종료 게이트이자 Stage2(6관점 LLM) 진입 전제.
- 산출물:
  - **(a) 결정론 최종 커버리지표**: 분식 16사 각 분식계정이 결정론 신호로 잡히는지 전수 재측정
    (S1~S10 누적 효과) + 정상 10사 거짓양성. "결정론이 후보로 띄우는 것"의 최종 목록.
  - **(b) LLM 적극 배분 영역** — 기준은 "결정론이 못 잡는 잔여물"(소거)이 **아니라**, **검색·맥락분석으로
    LLM이 판단하는 게 결정론보다 나은 것**(적극 배분)이다. 각 항목은 *LLM이 무엇을 검색·분석해 판단하는가*로 정의:
    · **소급재작성** → 정정공시·회계정책 주석·사업보고서를 **검색**해 회계정책변경/중단영업(IFRS5)/EPS
      소급재계산/오류수정/사업결합 잠정조정 **vs 분식성**(이익·자산 과대 후 하향)으로 **분류·판단** → change
    · **무형자산·개발비 자본화** → 원문 주석 + 동종업계 자본화율을 검색·분석해 과대자본화 판단 → note/industry
    · **절대 수준 이상(DIO·진행률)** → 업종 맥락·동종 비교로 "높게 유지된" 이상의 정상/비정상 판단 → numeric/flow
    · **연결범위 변경** → 주요사항보고서·지분 변동 검색으로 부당 편입 판단 → flow/change
    · **KAM·감사의견·정정공시** → 감사인 지적·정정 사유 해석 → external/note
  - **(c) 에이전트 판단 → 사용자 동의 루프 (human-in-the-loop, PLAN §15 포지셔닝)**:
    LLM은 검색·분석으로 **분식 의심 근거 + 정상 설명 + 반대근거 + 확인질문**을 제시하되 **확정하지 않는다.**
    사용자(감사인)가 근거를 보고 **동의·확인**해야 다음으로 간다. 도구는 "이건 정상 소급으로 보입니다 /
    이건 분식 의심입니다(근거 X)"를 제시하고 **판단·확정은 사람**이 한다. (결정론 = 빠짐없는 후보 발굴,
    LLM = 검색·분석 기반 의견 제시, 사람 = 최종 확인.)
- 완료기준(전수): 16사 결정론 최종 발굴율 + 정상 거짓양성 재측정값을 표로 고정. **16사 각 분식이
  (결정론이 잡음 / LLM에 인계) 중 어디로 가는지 전부 매핑**해 빈칸이 없을 것.
- 상태: **[x]**(2026-06-15, contract 67차). 산출물 `docs/agent/PHASE1_EXIT_GATE.md`. 결정론 recall **5/6**·
  거짓양성 0(삼성 아티팩트0·KAI 0). 분식 6사 전부 (결정론 잡음/LLM 인계) 매핑 빈칸 0 — miss 1건
  (세토피아 BW)도 S9 정정·S10 event·S7 원문 검색으로 LLM 인계 명시. S7~S10 신규수집이 결정론 약점(세토피아·디아이동일) 보강 지점 확인.

> **PHASE1 → Stage2 전환**: S11 통과 = 결정론 PHASE1 종료. 이후 Stage2는 결정론 후보(큐) + (b) 인계
> 목록을 6관점 LLM이 받아 판별한다. 결정론이 못 잡는 분식은 "도구 한계"가 아니라 "LLM이 받을 몫"으로
> S11에 명시돼 있어야 한다(조용한 누락 금지).

---

## 진행 규율
- 한 단계 = 착수 시 "셀 수 있는 산출물" 정의 → 완료 시 전수 재현값으로 대조.
- 범위 축소 시 본 문서 상태란에 "여기까지 측정, 나머지 X 미측정" 명시.
- 단계 완료마다 본 문서 상태 [x] 갱신 + STATE.md 반영.
