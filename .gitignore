# 환경/시크릿
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# 데이터 (회사별 격리 DB·원본 공시)
data/companies/
*.duckdb

# 백테스트 파싱 원문 (감리지적 원천에서 재생성 가능, 용량 큼)
# clues.jsonl·labels.csv 등 구조화 산출물은 추적 (정답지/포트폴리오 자산)
data/backtest/raw_text/

# 캐시 (OpenDartReader corp code 등 재생성 가능)
docs_cache/
*.pkl

# OS
.DS_Store
Thumbs.db

# 데이터 백업(재생성 가능·대용량) / 에이전트 작업상태·설정
data/_backup_corrected/
.claude/state/
.codex/

# 브라우저 자동화 산출물(로그·스냅샷, 재생성 가능)
.playwright-cli/
